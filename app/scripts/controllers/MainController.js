todo.controller('MainController', ['$scope', 'storageService', function($scope, storageService) {
    'use strict'
    //TODO
    // $scope.cateList = storageService.getData('cates');
    // $scope.listList = storageService.getData('lists');
    // $scope.taskList = storageService.getData('tasks');

}]);
